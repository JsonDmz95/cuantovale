import React, { Fragment } from 'react';
import { MainTitle } from "./StyledComponents";

const MemoryContainer = ({memoria}) => {
  return (
    <Fragment>
      <MainTitle>Propiedades Estimadas</MainTitle>
    </Fragment>
  );
}
 
export default MemoryContainer;